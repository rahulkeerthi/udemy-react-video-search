import React, { Component } from 'react';

const VideoList = ( {videos }) => {
  render() {
    return (
      <div>
        {props.videos.length}
      </div>
    )
  };
};

export default VideoList;